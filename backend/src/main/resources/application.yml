spring:
  datasource:
    url: jdbc:postgresql://ep-restless-dew-aiymc9ap-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_7crnEjBW2ioK
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
  servlet:
    context-path: /api/v1

app:
  jwt:
    secret: swiftcare-dev-secret-key-minimum-32-characters-long
    expiry-ms: 86400000

paystack:
  secret-key: your_paystack_test_secret_key

ai:
  api-key: your_claude_or_openai_key_here
  model: claude-haiku-4-5

firebase:
  credentials-path: firebase-service-account.json